-- +goose Up

CREATE TABLE IF NOT EXISTS "indexer_queue" (
    "id" INTEGER NOT NULL UNIQUE GENERATED BY DEFAULT AS IDENTITY,
    "page_id" INTEGER NOT NULL,
    "site_id" INTEGER NOT NULL,
    PRIMARY KEY ("id")
);

CREATE TABLE IF NOT EXISTS "ranking_engine_queue" (
    "id" INTEGER NOT NULL UNIQUE GENERATED BY DEFAULT AS IDENTITY,
    "page_id" INTEGER NOT NULL,
    "site_id" INTEGER NOT NULL,
    PRIMARY KEY ("id")
);

ALTER TABLE "indexer_queue"
ADD FOREIGN KEY ("site_id") REFERENCES "sites" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION;

ALTER TABLE "indexer_queue"
ADD FOREIGN KEY ("page_id") REFERENCES "pages" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION;

ALTER TABLE "ranking_engine_queue"
ADD FOREIGN KEY ("site_id") REFERENCES "sites" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION;

ALTER TABLE "ranking_engine_queue"
ADD FOREIGN KEY ("page_id") REFERENCES "pages" ("id") ON UPDATE NO ACTION ON DELETE NO ACTION;